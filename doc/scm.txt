
echo "# RUST_DEMO_WASM" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/aperezNWO/RUST_DEMO_WASM.git
git push -u origin main


my-rust-project/
├── Cargo.toml
├── Cargo.lock
├── src/
│   ├── main.rs        # Main entry point / REST server setup
│   └── fractals.rs    # Place the translated fractal engine code here
└── target/            # Build artifacts (ignored by git)

cargo new --lib rust_demo_wasm
wasm-pack build --target web            